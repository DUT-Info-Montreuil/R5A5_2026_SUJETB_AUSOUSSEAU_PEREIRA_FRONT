# Environnement de développement / test (chargé par `npm run dev` uniquement).
# « /api » est proxifié par Vite vers http://localhost:3000/api (voir vite.config.ts) :
# pas de souci de CORS, et ce proxy n'existe jamais dans le build de production.
VITE_API_URL=/api
