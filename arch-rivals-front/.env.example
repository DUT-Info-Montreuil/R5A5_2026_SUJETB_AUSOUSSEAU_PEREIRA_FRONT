# URL de base de l'API back.
# - Dev : déjà fournie par .env.development (« /api », proxifié vers http://localhost:3000/api).
# - Prod : à définir dans .env.production (non versionné) ou dans la CI, ex. https://api.exemple.fr/api
VITE_API_URL=/api
